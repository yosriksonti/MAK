<form method="post" action="{{ path('auto_delete_picture', {'id': picture.id}) }}">
    <button class="btn" ><i class="fa-solid fa-trash"></i></button>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture.id) }}">
</form>

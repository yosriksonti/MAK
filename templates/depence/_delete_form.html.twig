<form method="post" action="{{ path('depence_delete', {'id': depence.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depence.id) }}">
    <button class="btn"><i data-feather="trash"></i></button>
</form>

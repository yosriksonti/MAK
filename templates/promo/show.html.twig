{% extends "dashboard/index.html.twig" %}


<!DOCTYPE html>

<title>Promo</title>

{% block body %}
    <h1>Promo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promo.id }}</td>
            </tr>
            <tr>
                <th>Pourcentage</th>
                <td>{{ promo.Pourcentage }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ promo.Code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_promo_index') }}">back to list</a>

    <a href="{{ path('app_promo_edit', {'id': promo.id}) }}">edit</a>

    {{ include('promo/_delete_form.html.twig') }}
{% endblock %}
